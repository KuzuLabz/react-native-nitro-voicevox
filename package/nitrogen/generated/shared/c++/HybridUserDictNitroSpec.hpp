///
/// HybridUserDictNitroSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `UserDictWordInput` to properly resolve imports.
namespace margelo::nitro::voicevox { struct UserDictWordInput; }
// Forward declaration of `HybridUserDictNitroSpec` to properly resolve imports.
namespace margelo::nitro::voicevox { class HybridUserDictNitroSpec; }

#include <string>
#include "UserDictWordInput.hpp"
#include <NitroModules/Promise.hpp>
#include <memory>
#include "HybridUserDictNitroSpec.hpp"

namespace margelo::nitro::voicevox {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `UserDictNitro`
   * Inherit this class to create instances of `HybridUserDictNitroSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridUserDictNitro: public HybridUserDictNitroSpec {
   * public:
   *   HybridUserDictNitro(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridUserDictNitroSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridUserDictNitroSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridUserDictNitroSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::string addWord(const UserDictWordInput& word) = 0;
      virtual void updateWord(const std::string& wordUuid, const UserDictWordInput& word) = 0;
      virtual void removeWord(const std::string& wordUuid) = 0;
      virtual std::shared_ptr<Promise<std::string>> getWords() = 0;
      virtual std::shared_ptr<Promise<void>> load(const std::string& dictUrl) = 0;
      virtual std::shared_ptr<Promise<void>> save(const std::string& destUrl) = 0;
      virtual std::shared_ptr<Promise<void>> importDict(const std::shared_ptr<HybridUserDictNitroSpec>& other) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "UserDictNitro";
  };

} // namespace margelo::nitro::voicevox
